# Production environment settings
# (will be overridden by environment variables in the cloud)
spring:
  jpa:
    show-sql: false

jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION}
  refresh-expiration: ${JWT_REFRESH_EXPIRATION}

app:
  security:
    immediate-revocation: ${IMMEDIATE_REVOCATION}